#!/usr/bin/env -S deno run --allow-all

let exitCode = 0;

const lint = await new Deno.Command('npm', {
  args: ['run', 'lint'],
  stdout: 'piped',
  stderr: 'piped',
  env: {
    ...Deno.env.toObject(),
    FORCE_COLOR: '1',
  },
}).output();
if (lint.code !== 0) {
  await Deno.stdout.write(lint.stderr);
  exitCode = 1;
}

const tsc = await new Deno.Command('tsc', {
  args: ['--project', 'tsconfig.json', '--noEmit'],
  stdout: 'piped',
  stderr: 'piped',
  env: {
    ...Deno.env.toObject(),
  },
}).output();
if (tsc.code !== 0) {
  await Deno.stdout.write(tsc.stderr);
  exitCode = 1;
}

Deno.exit(exitCode);
